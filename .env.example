# Copy to apps/web/.env.local (or run: npm run setup)
# URL and anon key MUST be from the same Supabase project (Settings → API)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=
VITE_VAPID_PUBLIC_KEY=
